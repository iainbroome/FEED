{{#if @page.show_title_and_feature_image}}
	<section class="section-hero">
		<div class="post-media">
			{{> "post/featured-image" fast=true}}
		</div>

		<h1>{{title}}</h1>
		{{#if custom_excerpt}}
			<p>{{custom_excerpt}}</p>
		{{/if}}
	</section>
{{/if}}